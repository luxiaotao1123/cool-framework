package @{COMPANYNAME}.@{PACKAGENAME}.controller;

import @{COMPANYNAME}.@{PACKAGENAME}.service.@{ENTITYNAME}Service;
import @{COMPANYNAME}.@{PACKAGENAME}.entity.@{ENTITYNAME};
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.PathVariable;

@Controller
@RequestMapping("/@{SIMPLEENTITYNAME}")
public class @{ENTITYNAME}Controller {

    @Autowired
    private @{ENTITYNAME}Service @{SIMPLEENTITYNAME}Service;

    @RequestMapping(value = "/{id}")
    @ResponseBody
    public @{ENTITYNAME} get(@PathVariable("id") Long id) {
        return @{SIMPLEENTITYNAME}Service.selectById(String.valueOf(id));
    }

	@RequestMapping(value = "/update")
    @ResponseBody
    public String update(@{ENTITYNAME} @{SIMPLEENTITYNAME}){
        return String.valueOf(@{SIMPLEENTITYNAME}Service.updateById(@{SIMPLEENTITYNAME}));
    }
}